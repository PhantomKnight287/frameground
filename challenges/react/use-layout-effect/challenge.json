{
  "$schema": "../../schema.json",
  "id": "use-layout-effect",
  "setup_commands": [
    "pnpm i",
    "clear"
  ],
  "test_runner": "vitest",
  "track_slug": "react",
  "author": [
    "phantomknight287"
  ],
  "description": "Measure and mutate the DOM before the browser paints with useLayoutEffect, so the user never sees a flicker.",
  "difficulty": "medium",
  "label": "Layout Effects",
  "playground_needed": true,
  "prerequisites": [
    "react/use-effect",
    "react/use-ref"
  ]
}
